name: Content Tsunami - DeepSeek LOCAL (3x Daily)

on:
  schedule:
    - cron: '0 6 * * *'   # 6 AM
    - cron: '0 12 * * *'  # 12 PM
    - cron: '0 18 * * *'  # 6 PM
  workflow_dispatch:

jobs:
  # This workflow is designed for LOCAL execution
  # GitHub Actions doesn't support Ollama, so this needs to run on your machine

  local-reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Reminder for local execution
        run: |
          echo "⚠️  This workflow requires Ollama + DeepSeek running LOCALLY"
          echo "📝 To run locally, execute:"
          echo "    cd backend && python content_tsunami_deepseek.py"
          echo ""
          echo "🤖 Or set up a local cron job to run automatically"
          echo "💯 100% FREE - No API costs!"
